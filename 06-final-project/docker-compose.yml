version: '3.8'

services:
  agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - AGENT_API_KEY=demo-secret-key
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
